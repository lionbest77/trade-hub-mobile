import {StyleSheet} from "react-native";

export  const styles = StyleSheet.create({
  container: {
    backgroundColor: "#FFF",
    flex: 1
  },
  mainContainer: {
    flex: 1,
    alignItems: 'center',
    paddingBottom: 25,
    backgroundColor: "#FFF",
  },
 });
